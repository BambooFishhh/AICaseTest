global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: aicasetest-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - backend:8000
