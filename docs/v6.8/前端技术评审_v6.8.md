# 前端技术评审 v6.8

> 版本 v6.8，一旦确定尽量不要轻易改动。主题：队列与多实例（后端）。

## 1. 变更点

- 本版本无前端源码变更。
- 任务中心在 v6.7 已上线，v6.8 仅调整后端调度/运行态存储。

## 2. props / emit 变化

无。

## 3. 数据流

- 前端任务中心继续使用 `/api/admin/tasks`，语义不变。
- 执行/分析接口行为不变，取消与互斥改由 Redis 运行态承载。

## 4. 向后兼容性

- 所有接口字段不变。

## 5. 测试验证方案

- `npm run build` 回归通过。
