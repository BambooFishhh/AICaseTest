# 后端技术评审文档 v1.8 — 用例评审状态流转

**版本**: v1.8（后端）
**基线**: v1.7
**日期**: 2026-08-09
**原则**: 一旦确定尽量不要轻易改动；如需变更需升版本号并记录

---

## 一、改动点

### 1.1 TestCase entity 新增 reviewStatus

文件：`backend/src/main/java/com/testagent/entity/TestCase.java`

```java
// v1.8: 评审状态 draft/reviewed/approved/rejected
@Column(name = "review_status")
private String reviewStatus = "draft";
```

H2 DDL-auto 自动加列，历史行 null。DTO 层兜底为 draft。

### 1.2 TestCaseDTO 透传 + 兜底

文件：`backend/src/main/java/com/testagent/dto/TestCaseDTO.java`

```java
private String reviewStatus;

// from() 中：
.reviewStatus(entity.getReviewStatus() == null ? "draft" : entity.getReviewStatus())
```

### 1.3 新增 ReviewRequest DTO

文件：`backend/src/main/java/com/testagent/dto/ReviewRequest.java`

```java
@Data
public class ReviewRequest {
    private List<String> ids;
    private String status;      // draft/reviewed/approved/rejected
    private String reviewer;   // 可选
}
```

### 1.4 TestCaseController 新增 /review 接口 + listTestCases 加参数

文件：`backend/src/main/java/com/testagent/controller/TestCaseController.java`

```java
@GetMapping
public ApiResponse<TestCaseListResponse> listTestCases(
        @PathVariable String projectId,
        @RequestParam(defaultValue = "1") int page,
        @RequestParam(defaultValue = "20") int pageSize,
        @RequestParam(required = false) String type,
        @RequestParam(required = false) String module,
        @RequestParam(required = false) String keyword,
        @RequestParam(required = false) String reviewStatus) {  // v1.8
    return ApiResponse.success(testCaseService.listTestCases(
            projectId, page, pageSize, type, module, keyword, reviewStatus));
}

@PostMapping("/review")
public ApiResponse<Map<String, Object>> reviewTestCases(
        @PathVariable String projectId,
        @RequestBody ReviewRequest req) {
    return ApiResponse.success(
            testCaseService.batchUpdateReviewStatus(projectId, req.getIds(), req.getStatus(), req.getReviewer()));
}
```

需新增 import：`com.testagent.dto.ReviewRequest`、`java.util.Map`（已存在则忽略）。

### 1.5 TestCaseService 改造

文件：`backend/src/main/java/com/testagent/service/TestCaseService.java`

**listTestCases 签名加 reviewStatus 参数 + 过滤逻辑：**

```java
public TestCaseListResponse listTestCases(String projectId, int page, int pageSize,
                                           String type, String module, String keyword,
                                           String reviewStatus) {  // v1.8
    List<TestCase> all = testCaseRepository.findByProjectId(projectId);

    // v1.8: reviewStatus 筛选（null 视为 draft 兼容历史数据）
    if (reviewStatus != null && !reviewStatus.isBlank()) {
        all = all.stream().filter(tc -> {
            String rs = tc.getReviewStatus() == null ? "draft" : tc.getReviewStatus();
            return reviewStatus.equals(rs);
        }).collect(Collectors.toList());
    }
    // ... 原有 type/module/keyword 过滤 ...
}
```

**新增 batchUpdateReviewStatus：**

```java
private static final Set<String> VALID_REVIEW_STATUSES =
        Set.of("draft", "reviewed", "approved", "rejected");

@Transactional
public Map<String, Object> batchUpdateReviewStatus(String projectId, List<String> ids,
                                                     String status, String reviewer) {
    if (status == null || !VALID_REVIEW_STATUSES.contains(status)) {
        throw BusinessException.invalidParam("非法的评审状态: " + status);
    }
    if (ids == null || ids.isEmpty()) {
        throw BusinessException.invalidParam("ids 不能为空");
    }
    List<TestCase> all = testCaseRepository.findByProjectId(projectId);
    int updated = 0;
    for (TestCase tc : all) {
        if (ids.contains(tc.getId())) {
            tc.setReviewStatus(status);
            testCaseRepository.save(tc);
            updated++;
        }
    }
    Map<String, Object> result = new LinkedHashMap<>();
    result.put("updated", updated);
    result.put("status", status);
    if (reviewer != null && !reviewer.isBlank()) {
        result.put("reviewer", reviewer);
    }
    return result;
}
```

需新增 import：`com.testagent.dto.ReviewRequest`（controller）、`java.util.Set`（service，已存在）。

### 1.6 调用方适配
- `ProjectService` 或其他调用 `listTestCases` 的地方需补 reviewStatus 参数。需全局搜索 `listTestCases(` 调用点，补传 `null`。

---

## 二、改动文件清单

| 文件 | 改动 |
|------|------|
| `entity/TestCase.java` | 新增 reviewStatus 字段（默认 draft） |
| `dto/TestCaseDTO.java` | 透传 reviewStatus + null 兜底 draft |
| `dto/ReviewRequest.java` | 新增 DTO |
| `controller/TestCaseController.java` | listTestCases 加 reviewStatus 参数；新增 /review 接口 |
| `service/TestCaseService.java` | listTestCases 加参数 + 过滤；新增 batchUpdateReviewStatus |

---

## 三、API 契约变更

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/projects/{id}/testcases?reviewStatus=draft` | 新增筛选参数 |
| POST | `/api/projects/{id}/testcases/review` | 新增：批量改评审状态，body `{ids, status, reviewer?}` |

TestCaseDTO 新增字段 `reviewStatus`。

---

## 四、向后兼容

- 新增字段，H2 自动加列，历史行 null
- DTO 兜底 draft，前端无感知
- listTestCases 新参数可选，不传则不过滤
- 重新生成用例默认 draft（设计如此）

---

## 五、测试验证

- 后端编译：`mvn compile`
- 新生成用例 reviewStatus=draft
- 批量改状态后查询状态正确
- 按 reviewStatus 筛选正确
- 历史用例（null）按 draft 筛选/显示
