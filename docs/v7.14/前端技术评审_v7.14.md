# 前端技术评审 v7.14 — 生成 Prompt 重复注入治理（无前端变更）

> 版本 v7.14，一旦确定尽量不要轻易改动。基线 v7.13。本版**前端零改动**（回归构建）。

## 一、变更点
无前端文件变更。本版为生成 Prompt 重复注入治理 + embedding 端点修正（E17：chat 网关不提供 `qwen3.7-text-embedding`，RAG 检索持续降级为空），属后端 `TestGeneratorAgent` prompt 组装 / LLM 配置改动。

详见 `docs/v7.14/后端技术评审_v7.14.md`。

## 二、文件变更清单
无前端文件。

## 三、前端契约变化
无。

## 四、验证结果
后端全量回归绿；`npm run build` 通过（前端零改动回归）。

## 五、遗留
无（纯后端；embedding 端点修正后前端 RAG 相关上下文质量恢复，前端无感知）。