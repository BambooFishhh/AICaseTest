# 前端技术评审 v8.3 — 覆盖率口径重构

> 对应 PRD：PRD_v8.3_覆盖率口径重构.md
> 一旦确定尽量不要轻易改动

## 1. 改动总览

覆盖率视图全面切换"本期口径"语义 + 未建范围引导态。布局结构不变，改文案、空态与标记。

## 2. 修改文件

### 2.1 views/TestCaseList.vue

- 统计卡：`coverage?.scoped === false` 时两张覆盖卡显示 '—'，tooltip 提示"请先创建并确认本期范围"
- CoverageMatrix 容器：scoped=false 时渲染 el-alert 引导（链接到 /projects/:id/scope）替代矩阵

### 2.2 components/CoverageMatrix.vue

- 头部说明改为"分母 = 已确认本期范围（目标接口 + 本期转换）；历史转换仅展示不参与统计"
- transition 行：`inScope === false` 显示灰色 tag「历史」且覆盖单元格显示 '-'

### 2.3 views/StateMachineOverview.vue

- loadCoverageMatrix 响应 scoped=false → 页面顶部 el-warning 引导先建范围；覆盖卡片隐藏

### 2.4 views/Dashboard.vue

- projectCoverage 项 rate 为 null 时显示 '—'（后端未 scoped 返回 null）

## 3. 数据流

无新增 API 调用；响应新增字段 scoped/inScope/affectedItems 沿既有加载管道。

## 4. 测试验证

1. npm run build 通过
2. 无范围项目：列表统计卡 '—'、矩阵区引导 alert
3. 有范围项目：矩阵正常、历史转换灰标、affectedItems 可见
