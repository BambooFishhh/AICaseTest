# 前端技术评审 v7.15

> 一旦确定尽量不要轻易改动
> 基线：v7.14 | 主题：执行可信与双编号制（前端侧）

## 1. 变更点

### 1.1 流式草稿 upsert 兜底（TestCaseList.vue）

```js
// v7.15: 流式草稿按标题 upsert——后端已做跨轮推送去重，这里兜底防同题堆卡
function upsertStreamedCase(tc) {
  const key = String(tc?.title ?? '').trim().toLowerCase()
  const idx = streamedCases.value.findIndex(
    (d) => String(d?.title ?? '').trim().toLowerCase() === key)
  if (idx >= 0) streamedCases.value.splice(idx, 1)
  streamedCases.value.unshift(tc)
}
```

- 全量生成与追加生成两处 `onCase` 统一改为 `onCase: upsertStreamedCase`。
- 与后端 wrapPushDedup 互为冗余：任一侧漏网，界面都不会堆卡。

### 1.2 双编号展示（TestCaseList.vue 编号列）

```html
<el-tooltip v-else :content="'平台编号 ' + row.id" placement="top">
  <span class="case-id">#{{ row.projectSeq ?? row.id }}</span>
</el-tooltip>
```

- 主显项目内序号 `#N`；悬浮显示平台全局 TC-xxx。
- `projectSeq` 为空时回退显示全局 id（兼容旧数据/流式草稿场景）。

### 1.3 未覆盖接口清单面板（TestCaseList.vue + api/coverage.js）

- 新 API 封装 `getUncoveredEndpoints(projectId)`。
- 数据加载并入 `loadCoverageMatrix()`——该函数是全部刷新点的既有汇聚处
  （挂载、生成完成/取消/断连、批量操作后），零额外接线。
- 新增折叠面板：收起态显示"未覆盖接口 N / M"与提示语；展开列出 method tag + path + 描述，
  最大高度 240px 内滚动。

### 1.4 覆盖率口径标注（3a）

- "状态机覆盖 / 接口覆盖"两个统计卡 label 包裹 `el-tooltip` 说明各自分母与计入规则；
- 卡片下新增 `.coverage-caliber-note` 一行说明两口径不可比；
- CoverageMatrix.vue 矩阵描述补"（分母为状态机全部转换）"。

### 1.5 版本标注泄漏清理与保存联动

- CoverageMatrix.vue / TestCaseList.vue 用户可见文案中的 `（v7.8 双栏口径）`、`（v3.13 起强制过滤）`
  移除（版本标注只应存在于注释与 CHANGELOG）；
- PrdPanel 新增 `saved` emit，ProjectDetail 监听后 `refreshProject()`——保存 PRD 后
  "生成用例"按钮即时解禁，无需手动刷新页面。

## 2. props/emit 变化

| 组件 | 变化 |
|---|---|
| PrdPanel.vue | 新增 `emits: ['saved']`；persistDocs 成功后触发 |
| ProjectDetail.vue | `<PrdPanel @saved="refreshProject" />` |
| TestCaseList.vue | 无对外接口变化；新增内部 ref：uncoveredEndpoints / uncoveredOpen |

## 3. 数据流

- 未覆盖清单：`loadCoverageMatrix()`（串行追加请求，失败静默保留旧值）
  → `uncoveredEndpoints` → 模板折叠面板。
- 序号回填对前端透明：列表接口直接返回 `projectSeq`。

## 4. 向后兼容

- 老数据无 projectSeq 时编号列回退显示 TC-id；
- 未覆盖面板在 total=0 或接口异常时不渲染/保持旧值。

## 5. 测试验证

- vitest 7/7 通过；vite build 成功。
- 生产容器实测：编号列显示 #1…#N、悬浮见 TC-xxx；未覆盖面板显示 28/86 且展开列表正常。
