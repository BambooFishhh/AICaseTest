# 前端技术评审 v9.0 — ScopeDrawer 范围入口降级

> 版本 v9.0，一旦确定尽量不要轻易改动。基线 v8.9.8。

## 一、变更点

- ScopeDrawer.vue（+84/-x）：范围确认后「本期范围」从操作入口降级为**查看入口**——已确认范围放开条目增删（与后端放开接口同口径），主流程不再依赖手动确认；
- 空范围防护：识别结果为空时禁用「确认锁定」按钮并提示重算/手动添加（含 ac55fb0 的 UX 缺口修复：消除点确认才报后端"范围为空"错）；
- `git-refs` 返回的 `defaultBaseline` 预填基线输入框。

## 二、文件变更清单

frontend/src/components/ScopeDrawer.vue 修改。

## 三、验证结果

前端构建通过；litemall 实测确认范围后查看入口正常展示 11/11 条目。
