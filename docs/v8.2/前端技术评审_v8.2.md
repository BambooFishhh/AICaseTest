# 前端技术评审 v8.2 — 本期聚焦生成

> 对应 PRD：PRD_v8.2_本期聚焦生成.md
> 一旦确定尽量不要轻易改动

## 1. 改动总览

本期前端为轻量适配：phase 徽标 + blocked 状态文案。生成/执行主流程 UI 不变。

## 2. 修改文件

### 2.1 components/TestCaseCard.vue

structuredSteps 步骤列表渲染处：`step.phase === 'setup'` 时在步骤行前追加灰色 tag「准备」，其余（verify/缺省）不显示标记，避免视觉噪声。

### 2.2 执行状态文案映射

涉及 `views/ExecutionResult.vue`、`views/BatchResult.vue`、`views/ExecutionHistory.vue`、`views/TestCaseList.vue` 中已有的 status → 中文标签/tag type 映射表：

- 增加 `blocked: '已阻断'`（tag type: warning）
- 已存在 blocked 映射的不重复添加（TestCaseList 手动筛选可能已有）

### 2.3 views/ScopeReview.vue（v8.1 页面小改）

createDraft 响应含 `autoIdentified:false` 时 ElMessage 提示"该项目非 Git 仓库，已创建空草稿，请手动添加条目"。

## 3. 数据流

无新增 API；blocked 为 executionStatus/status 字段新取值，沿既有渲染管道。

## 4. 向后兼容

- phase 缺省的存量用例渲染不变
- blocked 仅在新语义触发时出现，旧数据不受影响

## 5. 测试验证

1. npm run build 通过
2. 用例详情：setup 步骤显示「准备」徽标；verify 步骤无徽标
3. 执行历史/结果页：blocked 记录显示"已阻断"黄色徽标
