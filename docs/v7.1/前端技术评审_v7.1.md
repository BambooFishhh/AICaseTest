# 前端技术评审 v7.1 — 生成链路一致性修复（G2 配套）

**改动文件**: `src/api/testcase.js`、`src/views/TestCaseList.vue`
**改动量**: 小（两个文件，无新组件）

---

## 1. src/api/testcase.js

### streamGenerate 的 complete 回调签名调整
```js
// v3.2（旧）:
es.addEventListener('complete', (e) => {
  try { onComplete?.(JSON.parse(e.data).total) } catch {}
  es.close()
})

// v7.1（新）: 传递完整对象，调用方按需取 total/pushed/droppedTotal/dropped
es.addEventListener('complete', (e) => {
  try { onComplete?.(JSON.parse(e.data)) } catch {}
  es.close()
})
```
- streamGenerateAppend 本就传完整对象（v3.5 起），本次对齐
- 调用方兼容性：streamGenerate 唯一消费方是 TestCaseList.vue 的重新生成流程，同步更新

## 2. src/views/TestCaseList.vue

### 2.1 重新生成 onComplete（G2 丢弃明细提示）
```js
onComplete: async (data) => {
  const total = data?.total ?? 0
  const pushed = data?.pushed ?? total
  const droppedTotal = data?.droppedTotal ?? Math.max(0, pushed - total)
  if (droppedTotal > 0) {
    const d = data?.dropped || {}
    const parts = []
    if (d.review) parts.push(`评审 ${d.review}`)
    if (d.dedup) parts.push(`去重 ${d.dedup}`)
    if (d.semantic) parts.push(`语义去重 ${d.semantic}`)
    if (d.focusType) parts.push(`类型过滤 ${d.focusType}`)
    if (d.other) parts.push(`其他 ${d.other}`)
    ElMessage.warning(`生成完成：落库 ${total} 条（流式推送 ${pushed} 条草稿，经 ${parts.join('、')} 丢弃 ${droppedTotal} 条）`)
  } else {
    ElMessage.success(`用例生成完成，共 ${total} 条`)
  }
  // 其余状态复位与刷新逻辑不变
}
```
用户不再看到"推送 30 条、列表 20 条"的无解释差异。

### 2.2 流式提示标注草稿（G2 前端侧）
```js
// streamingAlertTitle:
'已收到 N 条' → '已收到 N 条草稿（落库前经评审/去重可能调整）'
```
配合表格编号列既有的"生成中"占位，明确流式内容是草稿而非最终资产。

## 3. 兼容性

- 后端 complete 事件对旧前端是增量字段，本次前后端同批发布无脏窗口
- 无路由/状态管理/组件 props 变更；追加生成（streamGenerateAppend）流程不动
