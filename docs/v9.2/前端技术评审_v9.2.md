# 前端技术评审 v9.2 — 流式重影修复与执行结果展示优化

> 版本 v9.2，一旦确定尽量不要轻易改动。基线 v9.1。

## 一、变更点

- **TestCaseList.vue（+245）**：
  - **流式重影修复**：el-table row-key 原 `id`——流式草稿无持久化 id，key 全 undefined 树形合并冲突产生重影（组计数 N 但渲染行数 > N）。行唯一键改 `testCaseRowKey`：模块行 id / 草稿行 `draft-{seq}` 递增 renderKey / 落库行持久化 id；
  - **追加模式同名草稿隐藏**：草稿 title 键（剥离全部空白含 NBSP/零宽/全角，与后端 dedupTitleKey 同语义）命中库内旧用例则不重复展示，横幅显示"同名跳过 N 条"计数；
  - 执行环境前置步骤编辑改大弹窗编辑器。
- **ExecutionResult.vue（+15）**：截图/录屏改手机竖版比例展示（配合执行端 iPhone 14 设备模拟，媒体不再横向拉伸）。

## 二、文件变更清单

frontend/src/views/TestCaseList.vue、frontend/src/views/ExecutionResult.vue。

## 三、验证结果

前端构建通过；litemall 实测流式生成无重影、追加模式同名草稿不堆卡、执行结果媒体竖版正常。
