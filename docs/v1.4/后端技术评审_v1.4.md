# 后端技术评审文档 v1.4 — 生成质量增强II & 批量操作

**版本**: v1.4（后端）
**基线**: v1.3
**日期**: 2026-08-09
**评审范围**: LLM prompt 重构、LLM 重试、批量删除接口、批量导出接口
**原则**: 一旦确定尽量不要轻易改动；如需变更需升版本号并记录

---

## 一、改动点

### 1.1 TestGeneratorAgent — prompt 深度重构（F1+F2）

文件：`backend/src/main/java/com/testagent/agent/TestGeneratorAgent.java`

**现状**（第 106-117 行）：systemPrompt 为单行拼接字符串。

**v1.4**：重构为结构化分段常量 + few-shot 示例。

```java
private static final String SYSTEM_PROMPT = """
    # 角色
    你是资深测试工程师，擅长生成结构化、AI 可执行的测试用例。

    # 任务
    根据以下状态机和接口信息，为每个状态转换生成测试用例。

    # 生成要求
    ## 数量引导
    - 正向用例（positive）：每个合法状态转换至少 1 条
    - 异常用例（negative）：每个状态转换至少 1 条非法输入/非法转换
    - 边界值用例（boundary）：涉及数值/长度字段的至少 2 条（上界+下界）
    - 数据驱动用例（data）：多参数组合场景至少 1 条

    ## 测试数据要求
    - testData 必须包含具体字段值，不能为空对象 {}
    - 数值字段：填入真实值和边界值（如 amount: 0, amount: -1, amount: 99999999）
    - 字符串字段：填入正常值、空字符串、超长字符串（256字符）
    - 枚举字段：填入合法值和非法枚举值
    - 必填字段：测试缺失该字段的情况

    ## structuredSteps 要求
    - 每步的 target 必须是具体操作目标（如 "POST /api/order/create"），不能为空
    - 每步的 expected 必须是可验证的具体结果，不能为空
    - api_call 类型步骤的 data 必须包含该步骤的输入参数

    ## stateMachineRef 要求
    - transitions 数组必须包含本用例测试的状态转换
    - forbiddenTransitions 仅在 negative 类型用例中填写

    # 输出格式
    返回 JSON 数组，字段说明：
    - title: 用例标题（简洁，含测试目标）
    - module: 所属模块
    - type: positive/negative/boundary/data
    - priority: P0/P1/P2/P3
    - preconditions: 前置条件数组
    - steps: 步骤简述数组
    - expectedResults: 预期结果数组
    - structuredSteps: [{order, action, target, expected, data, type}]
    - apiEndpoints: [{method, path, description}]
    - testData: {字段名: 值}
    - executionHints: {approach, notes, prerequisites}
    - stateMachineRef: {states, transitions, forbiddenTransitions}

    只返回 JSON 数组，不要包含其他文字。
    """;

private static final String FEW_SHOT_EXAMPLES = """
    # 示例（参考质量标准，不要原样复制）
    [
      {
        "title": "创建订单-正常流程",
        "module": "订单管理",
        "type": "positive",
        "priority": "P0",
        "preconditions": ["用户已登录", "购物车有商品"],
        "steps": ["调用创建订单接口", "验证返回订单号", "验证订单状态为待支付"],
        "expectedResults": ["接口返回201和订单号", "订单状态=PENDING_PAYMENT"],
        "structuredSteps": [
          {"order":1,"action":"创建订单","target":"POST /api/order/create","expected":"返回201和orderId","data":{"userId":"U001","items":[{"skuId":"SKU001","quantity":2}],"amount":99.90},"type":"api_call"},
          {"order":2,"action":"验证订单状态","target":"GET /api/order/{orderId}","expected":"status=PENDING_PAYMENT","data":{},"type":"state_assert"}
        ],
        "apiEndpoints": [{"method":"POST","path":"/api/order/create","description":"创建订单"}],
        "testData": {"userId":"U001","amount":99.90},
        "executionHints": {"approach":"api_call","notes":"先创建再查询验证状态","prerequisites":["用户已登录"]},
        "stateMachineRef": {"states":[],"transitions":[{"from":"NONE","to":"PENDING_PAYMENT","trigger":"create"}],"forbiddenTransitions":[]}
      },
      {
        "title": "创建订单-金额为负数",
        "module": "订单管理",
        "type": "negative",
        "priority": "P1",
        "preconditions": ["用户已登录"],
        "steps": ["传入负数金额创建订单", "验证接口拒绝"],
        "expectedResults": ["接口返回400","错误消息提示金额非法"],
        "structuredSteps": [
          {"order":1,"action":"传入负数金额创建订单","target":"POST /api/order/create","expected":"返回400错误","data":{"userId":"U001","amount":-1},"type":"api_call"}
        ],
        "apiEndpoints": [{"method":"POST","path":"/api/order/create","description":"创建订单"}],
        "testData": {"userId":"U001","amount":-1},
        "executionHints": {"approach":"api_call","notes":"验证金额校验逻辑","prerequisites":["用户已登录"]},
        "stateMachineRef": {"states":[],"transitions":[],"forbiddenTransitions":[{"from":"PENDING_PAYMENT","to":"NONE","reason":"金额非法不可创建"}]}
      }
    ]
    """;
```

**调用变更**（`generateByLlmForStateMachine` 方法）：

```java
String userPrompt = "上下文信息：\n" + objectMapper.writeValueAsString(context)
        + "\n\n" + FEW_SHOT_EXAMPLES
        + "\n\n请基于上下文生成测试用例。";
```

### 1.2 LlmService — 重试机制（F3）

文件：`backend/src/main/java/com/testagent/service/LlmService.java`

**现状**（第 52-106 行）：`chat()` 无重试，任何异常直接抛出。

**v1.4**：将核心 HTTP 调用抽取为 `callLlmApi()`，在 `chat()` 中包装重试。

```java
private static final int MAX_RETRIES = 3;
private static final long[] RETRY_DELAYS_MS = {1000, 2000, 4000};

public String chat(String systemPrompt, String userPrompt, double temperature) {
    Exception lastException = null;
    for (int attempt = 0; attempt < MAX_RETRIES; attempt++) {
        try {
            return callLlmApi(systemPrompt, userPrompt, temperature);
        } catch (BusinessException e) {
            // 400/401 等客户端错误不重试
            if (e.getStatus() == HttpStatus.BAD_REQUEST
                    || e.getStatus() == HttpStatus.UNAUTHORIZED
                    || e.getStatus() == HttpStatus.FORBIDDEN) {
                throw e;
            }
            lastException = e;
            log.warn("LLM call attempt {} failed: {} (status={}), will retry",
                    attempt + 1, e.getMessage(), e.getStatus());
        } catch (Exception e) {
            lastException = e;
            log.warn("LLM call attempt {} failed: {}, will retry", attempt + 1, e.getMessage());
        }
        if (attempt < MAX_RETRIES - 1) {
            try {
                Thread.sleep(RETRY_DELAYS_MS[attempt]);
            } catch (InterruptedException ie) {
                Thread.currentThread().interrupt();
                break;
            }
        }
    }
    throw new BusinessException(50002,
            "LLM调用失败（已重试" + MAX_RETRIES + "次）: " + lastException.getMessage(),
            HttpStatus.INTERNAL_SERVER_ERROR);
}

private String callLlmApi(String systemPrompt, String userPrompt, double temperature) throws Exception {
    // 原 chat() 方法的 HTTP 调用逻辑
    // 构建 messages / requestBody / Request
    // 执行 httpClient.newCall(request).execute()
    // 解析返回 content
}
```

### 1.3 批量删除接口（F4）

文件：`backend/src/main/java/com/testagent/controller/TestCaseController.java`

```java
@DeleteMapping("/batch")
public ApiResponse<Integer> batchDeleteTestCases(
        @PathVariable String projectId,
        @RequestBody BatchDeleteRequest req) {
    int deleted = testCaseService.batchDeleteTestCases(projectId, req.getIds());
    return ApiResponse.success(deleted);
}
```

文件：`backend/src/main/java/com/testagent/dto/BatchDeleteRequest.java`（新增）

```java
public class BatchDeleteRequest {
    private List<String> ids;
    // getter/setter
}
```

文件：`backend/src/main/java/com/testagent/service/TestCaseService.java`

```java
@Transactional
public int batchDeleteTestCases(String projectId, List<String> ids) {
    List<TestCase> all = testCaseRepository.findByProjectId(projectId);
    int count = 0;
    for (TestCase tc : all) {
        if (ids.contains(tc.getId())) {
            testCaseRepository.delete(tc);
            count++;
        }
    }
    return count;
}
```

### 1.4 批量导出选中（F5）

文件：`backend/src/main/java/com/testagent/controller/MindMapController.java`（如存在）或 TestCaseController

mindmap 生成接口新增可选参数 `testcaseIds`：

```java
@PostMapping("/{projectId}/mindmap")
public ApiResponse<String> generateMindmap(
        @PathVariable String projectId,
        @RequestBody(required = false) MindMapRequest req) {
    String url = mindmapService.generateMindmap(projectId, req != null ? req.getTestcaseIds() : null);
    return ApiResponse.success(url);
}
```

MindMapService 中按 testcaseIds 过滤用例后再生成脑图。

---

## 二、改动文件清单

| 文件 | 改动 |
|------|------|
| `agent/TestGeneratorAgent.java` | systemPrompt 重构为分段常量 + few-shot 示例注入 |
| `service/LlmService.java` | `chat()` 增加重试逻辑，抽取 `callLlmApi()` |
| `controller/TestCaseController.java` | 新增 `DELETE /batch` 批量删除端点 |
| `dto/BatchDeleteRequest.java` | 新增 DTO |
| `service/TestCaseService.java` | 新增 `batchDeleteTestCases()` |
| `controller/MindMapController.java` | mindmap 接口新增 testcaseIds 参数 |
| `service/MindMapService.java` | generateMindmap 按 testcaseIds 过滤 |

---

## 三、API 契约变更

### 3.1 新增端点

```
DELETE /api/projects/{projectId}/testcases/batch
Body: {"ids": ["TC-001", "TC-002"]}
响应: ApiResponse<Integer>（删除数量）
```

### 3.2 扩展端点

```
POST /api/projects/{projectId}/mindmap
Body: {"testcaseIds": ["TC-001", "TC-003"]}  (可选，不传=全部)
```

---

## 四、向后兼容

- prompt 变更不影响 API 契约，仅影响生成内容质量
- LLM 重试对调用方透明，异常类型不变
- 批量删除是新增端点，不影响现有单条删除
- mindmap 的 testcaseIds 参数可选，不传时行为与 v1.3 一致

---

## 五、测试验证

- 后端编译：`mvn compile`
- prompt 验证：生成后检查 testData 非空、structuredSteps target 非空
- 重试验证：模拟超时场景（日志确认重试 + 最终回退）
- 批量删除：调用 batch 接口后列表中对应用例消失
- 批量导出：指定 testcaseIds 后脑图只含选中用例
