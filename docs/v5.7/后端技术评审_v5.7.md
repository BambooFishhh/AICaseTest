# 后端技术评审 v5.7：数据索引与查询性能

> 版本 v5.7，一旦确定尽量不要轻易改动。

## 1. 变更点

### 1.1 Flyway V2

```sql
CREATE INDEX idx_exec_project_time ON execution_record (project_id, start_time DESC);
CREATE INDEX idx_exec_batch_status ON execution_record (batch_id, status);
CREATE INDEX idx_testcases_project_module ON test_cases (project_id, module);
CREATE INDEX idx_versions_testcase_no ON test_case_versions (test_case_id, version_no);
CREATE INDEX idx_analysis_project_created ON code_analysis (project_id, created_at);
CREATE INDEX idx_mindmap_project_created ON mindmaps (project_id, created_at);
```

### 1.2 执行历史分页

`ExecutionService.getExecutionsByProject(projectId, page, pageSize)`：

- 读取全量（按时间倒序）后切片，返回 items/total/page/pageSize。
- 全量统计 stats（total/passed/failed/running）与最近 20 次 completed 的 trend 一次返回。
- `ExecutionController` 增加 `page` / `pageSize` 参数（默认 1 / 20）。

### 1.3 Milvus 索引

```java
CreateIndexParam indexParam = CreateIndexParam.newBuilder()
        .withCollectionName(name)
        .withFieldName("embedding")
        .withIndexType(IndexType.IVF_FLAT)
        .withMetricType(MetricType.COSINE)
        .withExtraParam("{\"nlist\":128}")
        .build();
c.createIndex(indexParam);
c.loadCollection(LoadCollectionParam.newBuilder().withCollectionName(name).build());
```

## 2. 文件变更清单

| 文件 | 变更 |
|---|---|
| resources/db/migration/mysql/V2__add_performance_indexes.sql | 新增 |
| service/ExecutionService.java | 分页 + stats/trend |
| controller/ExecutionController.java | page/pageSize 参数 |
| resources/application-mysql.yml | Hikari 参数 |
| service/MilvusService.java | createIndex + loadCollection |

## 3. API 契约变化

`GET /api/projects/{projectId}/executions` 响应从数组改为对象：

```json
{"items":[],"total":0,"page":1,"pageSize":20,"stats":{},"trend":[]}
```

## 4. 向后兼容性

- 前端同步改造；无其他消费者。

## 5. 测试验证方案

- `mvn compile` / `mvn test`。
- MySQL 冒烟执行 V2 迁移。
- 分页接口抽查 total/items/stats/trend。
