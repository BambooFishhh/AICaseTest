# 前端技术评审 v8.4 — 256k 上下文扩容与代码审查修复（无前端变更）

> 版本 v8.4，一旦确定尽量不要轻易改动。基线 v8.3。本版**仅后端改动，无前端变更**。

## 一、变更点
无前端文件变更。本版为 256k 上下文预算扩容与代码审查修复（`endpoints-context-max` 等预算提升、Git 克隆 SSRF 防护、流式重试端到端一致）。属后端分析/生成/安全改动。

> 注：流式重试端到端一致涉及"前端 retryReset 清草稿"，但该前端消费能力在 v8.5 落地，v8.4 仅后端保证重试后不再重复推送（前端无代码变更）。

详见 `docs/v8.4/后端技术评审_v8.4.md`。

## 二、文件变更清单
无前端文件。

## 三、前端契约变化
无。`retryReset` 事件契约已由更早版本定义，v8.4 不改前端。

## 四、验证结果
后端全量回归绿；前端回归构建通过（无改动）。

## 五、遗留
无（纯后端；前端 retryReset 消费见 v8.5）。