# 前端技术评审 v9.3 — 执行结果面包屑上一级改测试用例

> 版本 v9.3，一旦确定尽量不要轻易改动。基线 v9.2。

## 一、变更点

- **router/index.js**：ExecutionResult / BatchResult 路由 meta.breadcrumb 由 `['项目列表','项目详情',...]` 改为 `['项目列表','测试用例',...]`——执行从用例列表发起，面包屑上一级与页面返回按钮（本就指向用例页）同口径；
- **App.vue**：crumbLink 增加「测试用例」映射 → `/projects/:id/testcases`，面包屑「测试用例」可点直达。

## 二、文件变更清单

frontend/src/router/index.js、frontend/src/App.vue。

## 三、验证结果

前端构建通过；执行结果/批次结果页面包屑「测试用例」可点回用例列表。

## 四、备注

- 执行历史（ExecutionHistory）面包屑保持 `项目详情` 口径不变——它是与测试用例平级的二级导航入口。
- 编号乱序的展示侧无代码改动：根因与修复均在后端（见后端技术评审 v9.3 §1.2），前端分组渲染按新编号自然连续。
