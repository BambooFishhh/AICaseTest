# 前端技术评审 v9.1 — resumeGenerationIfActive 重写为 attach

> 版本 v9.1，一旦确定尽量不要轻易改动。基线 v9.0。

## 一、变更点

- **api/testcase.js**（+84/-x）：
  - 新增 `attachGenerate(projectId, handlers)`——请求 `generate-stream-attach` SSE 端点，事件名解析与 streamGenerate 共用一套 handler 适配器；
  - streamGenerate 事件解析层去重（原宽限期重连逻辑删除）。
- **views/ProjectDetail.vue**（+26/-x）：`resumeGenerationIfActive` 由"项目状态轮询兜底"重写为 attach——重进页面时调用 attach，回放持久化事件渲染进度与草稿、续播实况；连接层断开仍降级轮询（onDisconnect 保留）。

## 二、文件变更清单

frontend/src/api/testcase.js、frontend/src/views/ProjectDetail.vue。

## 三、验证结果

前端构建通过；实测生成中刷新/切页后重进，进度条与用例草稿无损恢复，完成后整表重载。
