# 前端技术评审 v8.6.1 — 无前端变更

> 版本 v8.6.1，一旦确定尽量不要轻易改动。基线 v8.5。

本版本为纯后端改动（任务 9.1–9.4：向量删除补偿/ShedLock 重放/周期对账/检索幽灵过滤），**前端无代码变更**。

- 新增管理端点 `/api/admin/vector/reconciliation` 仅作运维观测，本期不接前端页面（后续可观测版本如需可视化再接入）。
- API 契约无存量字段变化，前端零适配。
