# 后端技术评审 v3.17

**版本**：v3.17，一旦确定尽量不要轻易改动
**日期**：2026-08-12

## 变更点

### 1. 全局统计

- 新增 `StatsController`：`GET /api/stats/overview`，聚合项目/用例/执行/覆盖率/类型分布

### 2. 默认生成参数

- `SettingsService.getDefaultGenerationParams/updateDefaultGenerationParams`（system_settings key：default_generation_params）
- `ProjectService.createProject` 初始化默认参数到新项目 settings
- `SettingsController` 新增 `GET/PUT /api/settings/generation-params`

### 3. API 文档

- pom 新增 springdoc-openapi-starter-webmvc-ui 2.5.0

## 文件变更清单

| 文件 | 变更 |
|---|---|
| controller/StatsController.java | 新增 |
| service/SettingsService.java | 默认参数读写 |
| controller/SettingsController.java | 默认参数端点 |
| service/ProjectService.java | 创建项目初始化默认参数 |
| pom.xml | springdoc 依赖 |

## API 契约变化

- 新增 `GET /api/stats/overview`
- 新增 `GET/PUT /api/settings/generation-params`

## 测试验证

`mvn test` 预期全部通过。
