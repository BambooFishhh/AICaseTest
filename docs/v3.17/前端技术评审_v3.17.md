# 前端技术评审 v3.17

**版本**：v3.17，一旦确定尽量不要轻易改动
**日期**：2026-08-12

## 变更点

### 1. Dashboard.vue（新增）

- 路由 `/dashboard`；统计卡 + ECharts 饼图（类型分布）+ 柱状图（项目覆盖率）

### 2. Settings.vue

- 新增"默认生成参数"区（密度/温度/聚焦类型/默认 URL）
- 页头新增"API 文档"按钮 → `/swagger-ui/index.html`

### 3. App.vue

- 侧边栏新增"仪表盘"导航项
- 项目内页面显示"当前项目"二级导航（详情/用例/执行历史/分析/状态机/脑图）
- 顶栏改为面包屑（依赖 router meta.breadcrumb）

### 4. router/index.js

- 新增 `/dashboard` 路由；所有路由补充 `meta.breadcrumb`

### 5. TestCaseList.vue

- 用例详情 el-dialog → el-drawer（62% 宽）

## 数据流

- 仪表盘：getStatsOverview → 统计卡 + 图表
- 默认参数：设置页保存 → system_settings → 新建项目初始化

## 向后兼容性

纯新增；面包屑与二级导航不影响既有页面功能。
