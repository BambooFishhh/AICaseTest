# 后端技术评审文档 v1.6 — 高可用增强

**版本**: v1.6（后端）
**基线**: v1.5
**日期**: 2026-08-09
**原则**: 一旦确定尽量不要轻易改动；如需变更需升版本号并记录

---

## 一、改动点

### 1.1 Project entity 新增字段

文件：`backend/src/main/java/com/testagent/entity/Project.java`

```java
@Column(name = "error_message", columnDefinition = "TEXT")
private String errorMessage;

@Column(name = "progress")
private String progress;
```

H2 DDL-auto 会自动加列，向后兼容。

### 1.2 ProjectDTO 透传

文件：`backend/src/main/java/com/testagent/dto/ProjectDTO.java`

```java
private String errorMessage;
private String progress;

// from() 方法中:
.errorMessage(entity.getErrorMessage())
.progress(entity.getProgress())
```

### 1.3 ProjectRepository 新增更新方法

文件：`backend/src/main/java/com/testagent/repository/ProjectRepository.java`

```java
@Modifying
@Query("UPDATE Project p SET p.progress = :progress, p.updatedAt = CURRENT_TIMESTAMP WHERE p.id = :id")
void updateProgress(@Param("id") String id, @Param("progress") String progress);

@Modifying
@Query("UPDATE Project p SET p.status = :status, p.errorMessage = :errorMsg, p.progress = null, p.updatedAt = CURRENT_TIMESTAMP WHERE p.id = :id")
void updateStatusWithError(@Param("id") String id, @Param("status") String status, @Param("errorMsg") String errorMsg);
```

### 1.4 TestCaseService 生成逻辑增强

文件：`backend/src/main/java/com/testagent/service/TestCaseService.java`

`runGenerate()` 方法改动：

```java
@Async("generationExecutor")
public void runGenerate(String projectId, GenerateRequest req) {
    try {
        updateProjectStatus(projectId, "generating");
        projectRepository.updateProgress(projectId, "正在解析状态机...");

        List<StateMachine> stateMachines = stateMachineRepository.findByProjectId(projectId);

        // ... backend result 解析 ...

        // 分模块生成
        List<TestCase> allTestCases = new ArrayList<>();
        for (int i = 0; i < stateMachines.size(); i++) {
            StateMachine sm = stateMachines.get(i);
            projectRepository.updateProgress(projectId,
                String.format("正在生成第 %d/%d 个模块: %s", i + 1, stateMachines.size(), sm.getName()));
            // ... generate for this SM ...
        }

        projectRepository.updateProgress(projectId, "正在保存用例...");
        // ... save ...

        projectRepository.updateProgress(projectId, null); // 清除进度
        updateProjectStatus(projectId, "completed");

    } catch (Exception e) {
        log.error("Test case generation failed for project {}", projectId, e);
        String errorMsg = e.getMessage() != null ? e.getMessage() : e.getClass().getSimpleName();
        projectRepository.updateStatusWithError(projectId, "failed", errorMsg);
    }
}
```

### 1.5 ProjectService 并发提示优化

文件：`backend/src/main/java/com/testagent/service/ProjectService.java`

```java
public void triggerGenerate(String id, GenerateRequest req) {
    // ...
    if (!"analyzed".equals(status) && !"completed".equals(status)) {
        if ("generating".equals(status)) {
            throw BusinessException.invalidState("正在生成中，请等待当前任务完成");
        }
        throw BusinessException.invalidState("项目当前状态不允许生成测试用例: " + status);
    }
    // ...
}
```

### 1.6 logback-spring.xml 结构化日志

文件：`backend/src/main/resources/logback-spring.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeContext>true</includeContext>
            <includeMDC>true</includeMDC>
            <customFields>{"app":"AICaseTest"}</customFields>
        </encoder>
    </appender>

    <logger name="com.testagent" level="DEBUG" />
    <logger name="org.springframework" level="INFO" />
    <logger name="org.hibernate" level="WARN" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
```

### 1.7 pom.xml 新增 logstash 依赖

```xml
<dependency>
    <groupId>net.logstash.logback</groupId>
    <artifactId>logstash-logback-encoder</artifactId>
    <version>7.4</version>
</dependency>
```

---

## 二、改动文件清单

| 文件 | 改动 |
|------|------|
| `entity/Project.java` | 新增 errorMessage + progress 字段 |
| `dto/ProjectDTO.java` | 新增 errorMessage + progress 透传 |
| `repository/ProjectRepository.java` | 新增 updateProgress + updateStatusWithError |
| `service/TestCaseService.java` | runGenerate 增加进度更新和错误存储 |
| `service/ProjectService.java` | triggerGenerate 并发提示优化 |
| `resources/logback-spring.xml` | 新增：结构化日志配置 |
| `pom.xml` | 新增 logstash-logback-encoder 依赖 |

---

## 三、API 契约变更

### ProjectDTO 新增字段

```json
{
  "id": "abc123",
  "status": "generating",
  "errorMessage": null,
  "progress": "正在生成第 2/5 个模块: 订单状态机"
}
```

失败时：
```json
{
  "status": "failed",
  "errorMessage": "LLM API call failed after 3 retries: 401 Unauthorized",
  "progress": null
}
```

---

## 四、向后兼容

- 新增字段，H2 DDL-auto 自动加列
- errorMessage/progress 为 null 时不影响现有逻辑
- logback-spring.xml 新增文件，不影响默认配置

---

## 五、测试验证

- 后端编译：`mvn compile`
- 生成失败时 errorMessage 有值
- 生成过程中 progress 更新
