# 前端技术评审 v2.7 — Playwright MCP Server + PlaywrightRecordSkill

> 版本：v2.7 | 主题：Playwright MCP Server + PlaywrightRecordSkill | 基线：v2.6
> 一旦确定尽量不要轻易改动

## 无前端改动

v2.7 是后端基础设施（新建 Playwright MCP Server + PlaywrightRecordSkill），不涉及前端任何改动。

- 执行链路仍使用 BrowserSkill（Selenium），前端行为不变
- PlaywrightRecordSkill 在 v2.8 才接入执行链路
- 前端视频播放支持在 v2.9 实现

## 验证

前端无需重新构建。后端启动后，现有功能应正常工作。
