# 后端技术评审 v3.3 — 流式生成取消与落库保护

> 版本：v3.3 | 主题：流式生成取消 + 落库保护
> 基线：v3.2 | 日期：2026-08-11
> 一旦确定尽量不要轻易改动

## 一、变更点

### 1.1 新增 GenerationCancelledException

```java
package com.testagent.common;

/**
 * v3.3: 用例生成被取消时抛出。runGenerateStream catch 后跳过落库、恢复状态。
 */
public class GenerationCancelledException extends RuntimeException {
    public GenerationCancelledException(String message) {
        super(message);
    }
}
```

### 1.2 TestCaseService — 取消注册表 + runGenerateStream 改造

新增字段与方法：

```java
// v3.3: 取消标志注册表（projectId → cancelled flag）
private final java.util.concurrent.ConcurrentHashMap<String, AtomicBoolean> cancellationFlags = new java.util.concurrent.ConcurrentHashMap<>();

// v3.3: 取消生成（供 Controller 调用）。返回是否成功取消。
public boolean cancelGeneration(String projectId) {
    AtomicBoolean flag = cancellationFlags.get(projectId);
    if (flag != null) {
        flag.set(true);
        return true;
    }
    return false;
}
```

`runGenerateStream` 改造（关键 diff）：

```java
@Async("generationExecutor")
public void runGenerateStream(String projectId, SseEmitter emitter) {
    AtomicBoolean clientGone = new AtomicBoolean(false);
    AtomicBoolean cancelled = new AtomicBoolean(false);  // ← v3.3 新增
    // v3.3: 客户端断开同时置 cancelled（不只跳过 send，还要停止生成）
    emitter.onCompletion(() -> {
        clientGone.set(true);
        cancelled.set(true);
        log.info("SSE client disconnected: {}", projectId);
    });
    emitter.onTimeout(() -> {
        clientGone.set(true);
        cancelled.set(true);
        log.warn("SSE timeout: {}", projectId);
    });
    emitter.onError(t -> {
        clientGone.set(true);
        cancelled.set(true);
        log.warn("SSE error: {}", projectId, t);
    });
    // v3.3: 注册取消标志（供 cancel 端点触发）
    cancellationFlags.put(projectId, cancelled);

    try {
        // ... 前置校验 + updateProjectStatus("generating") ...

        List<TestCase> testCases = orchestratorAgent.generateStreaming(
                projectId, progressCb, caseCb, cancelled);  // ← 透传 cancelled

        // v3.3: 落库前最终检查
        if (cancelled.get()) {
            throw new GenerationCancelledException("用户取消生成");
        }

        progressCb.update("正在保存用例...");
        testCaseRepository.deleteAll(testCaseRepository.findByProjectId(projectId));
        for (TestCase tc : testCases) {
            tc.setProjectId(projectId);
            testCaseRepository.save(tc);
        }

        projectRepository.updateProgress(projectId, null);
        updateProjectStatus(projectId, "completed");
        sendSseEvent(emitter, clientGone, "complete", Map.of("total", testCases.size()));
        safeSseComplete(emitter, clientGone);
    } catch (GenerationCancelledException e) {                          // ← v3.3 新增
        log.info("Streaming generation cancelled for project {}", projectId);
        // 落库保护：跳过 deleteAll + save，保留旧用例
        projectRepository.updateProgress(projectId, null);
        // 恢复状态：有旧用例→completed，无→created
        restoreProjectStatus(projectId);
        sendSseEvent(emitter, clientGone, "cancelled",
                Map.of("message", "生成已取消，旧用例已保留"));
        safeSseComplete(emitter, clientGone);
    } catch (Exception e) {
        // ... 原有 error 处理 ...
    } finally {
        // v3.3: 清理取消标志，避免内存泄漏
        cancellationFlags.remove(projectId);
    }
}
```

新增 `restoreProjectStatus` helper：

```java
// v3.3: 取消后恢复项目状态（有旧用例→completed，无→created）
private void restoreProjectStatus(String projectId) {
    List<TestCase> existing = testCaseRepository.findByProjectId(projectId);
    String status = (existing != null && !existing.isEmpty()) ? "completed" : "created";
    updateProjectStatus(projectId, status);
}
```

### 1.3 OrchestratorAgent — 透传 cancelled

```java
// v3.3: generateStreaming 新增 cancelled 参数
public List<TestCase> generateStreaming(String projectId,
                                        TestGeneratorAgent.ProgressCallback progressCallback,
                                        TestGeneratorAgent.CaseCallback caseCallback,
                                        java.util.concurrent.atomic.AtomicBoolean cancelled) {
    GenContext ctx = loadGenerationContext(projectId, progressCallback);
    return testGeneratorAgent.generateStreaming(ctx.prdResult(), ctx.stateMachines(),
            ctx.backendResult(), ctx.frontendResult(), progressCallback, caseCallback, cancelled);
}
```

### 1.4 TestGeneratorAgent — 检查点 + 方法签名

新增 `checkCancelled` helper：

```java
// v3.3: 取消检查。cancelled 为 null（非流式调用）或 false 时跳过。
private void checkCancelled(java.util.concurrent.atomic.AtomicBoolean cancelled) {
    if (cancelled != null && cancelled.get()) {
        throw new GenerationCancelledException("用例生成已取消");
    }
}
```

`generateStreaming` 签名变更（新增 `cancelled` 参数）：

```java
public List<TestCase> generateStreaming(PrdAnalysisResult prdResult, List<StateMachine> stateMachines,
                                         BackendResult backendResult, FrontendResult frontendResult,
                                         ProgressCallback progressCallback, CaseCallback caseCb,
                                         java.util.concurrent.atomic.AtomicBoolean cancelled) {
    List<TestCase> result = new ArrayList<>();
    if (prdResult != null && !prdResult.isEmpty()) {
        checkCancelled(cancelled);  // ← 检查点
        try {
            result = generateByLlmWithPrd(prdResult, stateMachines, backendResult, frontendResult, caseCb, cancelled);
        } catch (GenerationCancelledException e) {
            throw e;  // 取消异常向上传播，不触发 fallback
        } catch (Exception e) {
            log.warn("PRD-driven streaming generation failed, fallback to code-driven: {}", e.getMessage());
            result = new ArrayList<>();
        }
    }
    if (result.isEmpty()) {
        result = generateCodeDrivenCases(stateMachines, backendResult, frontendResult, progressCallback, caseCb, cancelled);
    }
    // ... 质量评分、去重、编号 ...
}
```

`generateCodeDrivenCases` 签名变更 + 循环内检查点：

```java
private List<TestCase> generateCodeDrivenCases(List<StateMachine> stateMachines, BackendResult backendResult,
                                                FrontendResult frontendResult,
                                                ProgressCallback progressCallback, CaseCallback caseCb,
                                                java.util.concurrent.atomic.AtomicBoolean cancelled) {
    List<TestCase> result = new ArrayList<>();
    if (stateMachines != null && !stateMachines.isEmpty()) {
        int total = stateMachines.size();
        for (int i = 0; i < total; i++) {
            checkCancelled(cancelled);  // ← 每模块前检查
            StateMachine sm = stateMachines.get(i);
            // ... LLM 调用 ...
        }
    }
    // ...
}
```

`generateByLlmWithPrd` / `generateByLlmForStateMachine` 签名变更 + LLM 调用前检查：

```java
private List<TestCase> generateByLlmWithPrd(..., CaseCallback caseCb,
                                            java.util.concurrent.atomic.AtomicBoolean cancelled) throws Exception {
    // ... 构造 prompt ...
    checkCancelled(cancelled);  // ← LLM 调用前检查
    String response = llmService.chat(SYSTEM_PROMPT_PRD_DRIVEN, userPrompt, 0.4);
    // ...
}

private List<TestCase> generateByLlmForStateMachine(..., CaseCallback caseCb,
                                                     java.util.concurrent.atomic.AtomicBoolean cancelled) {
    // ... 构造 prompt ...
    checkCancelled(cancelled);  // ← LLM 调用前检查
    String response = llmService.chat(systemPrompt, userPrompt, 0.4);
    // ...
}
```

> **注意**：`generateByRulesForStateMachine` 不加 cancelled 参数。规则回退是兜底，执行极快，无需取消。

### 1.5 ProjectController — cancel 端点

```java
// v3.3: 取消流式生成
@PostMapping("/{projectId}/testcases/generate-cancel")
public ApiResponse<Map<String, Object>> cancelGenerate(@PathVariable String projectId) {
    boolean cancelled = testCaseService.cancelGeneration(projectId);
    return ApiResponse.success(Map.of("cancelled", cancelled));
}
```

## 二、文件变更清单

| 文件 | 变更 | 目的 |
|------|------|------|
| `common/GenerationCancelledException.java` | 新增 | 取消异常类型 |
| `service/TestCaseService.java` | 改 | 取消注册表 + cancelGeneration + runGenerateStream 取消逻辑 + restoreProjectStatus |
| `agent/OrchestratorAgent.java` | 改 | generateStreaming 透传 cancelled |
| `agent/TestGeneratorAgent.java` | 改 | checkCancelled + 4 个方法签名加 cancelled 参数 + 检查点 |
| `controller/ProjectController.java` | 改 | cancel 端点 |

## 三、API 契约变更

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/projects/{projectId}/testcases/generate-cancel` | 取消流式生成（v3.3 新增） |

SSE 新增事件类型：

| 事件 | data | 说明 |
|------|------|------|
| `cancelled` | `{"message": "生成已取消，旧用例已保留"}` | 生成被取消（v3.3 新增） |

## 四、向后兼容

- 非流式 `runGenerate` 不受影响（无 cancelled 参数，行为不变）
- `OrchestratorAgent.generate`（非流式）不受影响
- `TestGeneratorAgent.generate`（非流式）不受影响
- 原有 SSE 事件（progress/case/complete/error）不变，仅新增 cancelled 事件
- cancel 端点对无生成任务的项目返回 `cancelled: false`，不报错

## 五、测试验证

1. `mvn compile` 编译通过
2. 流式生成期间调 cancel 端点 → 生成停止、旧用例保留、项目状态恢复
3. 流式生成期间关闭页面（模拟 clientGone）→ 后端停止生成、旧用例保留
4. 正常生成完成 → 不受影响
5. 无生成任务时调 cancel → 返回 cancelled:false
