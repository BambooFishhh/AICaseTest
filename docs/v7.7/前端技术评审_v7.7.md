# 前端技术评审 v7.7 — 上下文精准投喂（无前端变更）

> 版本 v7.7，一旦确定尽量不要轻易改动。基线 v7.6。本版**前端无代码变更**（回归构建）。

## 一、变更点
无前端文件变更。本版为上下文精准投喂：前端上下文按 RAG 命中组件精筛、后端全量注入（兜底对称），`complete` 事件新增 `coverageCappedByLimit` 字段。属后端上下文装配逻辑；前端界面不改（`coverageCappedByLimit` 暂在 complete 事件 JSON，前端展示延至 v7.8）。

详见 `docs/v7.7/后端技术评审_v7.7.md`。

## 二、文件变更清单
无前端文件。

## 三、前端契约变化
`complete` 事件新增可选 `coverageCappedByLimit` 字段（不破坏既有字段）；无界面改动。

## 四、验证结果
后端全量回归绿；前端回归构建通过（无改动）。

## 五、遗留
`coverageCappedByLimit` 前端提示待 v7.8 落地。