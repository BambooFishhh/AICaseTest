# 容量基线报告（v8.9.5 首版 / v8.9.6 补并发数据，计划书任务 12.7）

> 首版：2026-08-26 ｜ 环境：本机 Docker compose 全栈（单实例 backend + MySQL/Redis/Milvus/MinIO）
> 工具：`perf/smoke-shortrequests.ps1`（顺序冒烟）、`perf/load-shortrequests.ps1`（8 worker 并发聚合）、`perf/k6/short-requests.js`（备用脚本）
> 结论先行：**短请求路径远未到瓶颈**（并发 202 RPS 下 P95=82ms 零失败）；任务吞吐天花板与水平扩容路径见对照表。

## 一、本次实测

### 1.1 顺序冒烟（300 请求 / 0 失败）

| 场景 | 交替 GET `/api/health`(免认证) 与 `/api/projects`(JWT) |
|---|---|
| 平均耗时 | **12.4 ms** |
| P50 / P95 / Max | 16 ms / **26 ms** / 52 ms |
| 顺序 RPS（含 PS 客户端开销） | 80.6 |

### 1.2 并发压测（v8.9.6 补充：8 worker × 150 = 1200 请求 / **0 失败** / 5.9s）

| 指标 | 值 |
|---|---|
| 并发 RPS | **202.4**（客户端为 PowerShell 进程池，服务端真实能力更高） |
| P50 / P95 / Max | 15 ms / **82 ms** / 103 ms |

口径说明：worker 为独立进程 + Invoke-RestMethod 客户端开销计入耗时，属保守口径；零失败与 P95<100ms 表明 Hikari(40) 对齐后连接竞争已消除（C1 修复效果印证）。

## 二、理论值对照（计划书 CR §9.4）

| 层级 | 理论判断 | 本次数据印证 |
|---|---|---|
| Web 接入层 | 修复 Hikari(40) 后短请求上千 QPS 无压力；SSE 异步不占工作线程 | ✅ P95=26ms、零失败，与"不是瓶颈"一致 |
| 任务吞吐层 | 单实例生成吞吐 ≈ 10–30 次/小时（LLM 耗时主导）；超发排队+503 快速拒绝 | 未测（需 mock LLM 供应商，见"待补"） |
| 水平扩展 | 调度互斥/认证/DB 权威源已实测（12.8 附录）；限流聚合=实例配额×实例数 | 双实例演练 ✅ |

## 三、待补项

| 场景 | 阻塞点 | 解除方式 |
|---|---|---|
| k6 并发三场景复跑 | 本机 grafana/k6 镜像拉取超时（PS 版已覆盖短请求并发口径） | 镜像源恢复后跑 `perf/k6/short-requests.js` 对照 |
| 50 条 SSE 生成 / 混合场景 | 消耗真实 LLM token `[需确认]`——需 mock LLM 供应商 | 配 mock 供应商后补 `sse-generation.js` |
| 双实例 SSE 断线重连演练 | 同上（12.8 附录第 5 项） | 同上 |
| 对外并发量承诺 | 以上完成后回填本报告 | — |

## 四、上线自查清单回填（CR §9.4 最小集）

1. ✅ 12.3/12.4 凭据与 MCP 纵深完成
2. ✅ 12.1/12.2 连接池对齐 + LLM 限流完成
3. 🔄 12.7 压测：短请求冒烟已得首组数字；并发/SSE 场景待补
4. ✅ 前端 retryReset 已消费（8.5）
5. ✅ `.env` 密钥齐备（SecurityKeyGuard 启动校验通过）+ Grafana 告警规则生效（promtool 通过 + provisioning 加载）
