# 前端技术评审 v6.9

> 版本 v6.9，一旦确定尽量不要轻易改动。主题：任务回放展示。

## 1. 变更点

- `api/task.js` 新增 `getTaskTimeline(id)`。
- `views/TaskCenter.vue` 详情抽屉新增 timeline 回放（el-timeline）。

## 2. props / emit 变化

无外部组件契约变化。

## 3. 数据流

- 打开详情时并行请求任务详情与 timeline。
- 后端按时间正序返回，前端倒序展示最新在前。

## 4. 向后兼容性

- 任务中心既有详情/重试逻辑不变。

## 5. 测试验证方案

- `npm test` / `npm run build` 通过。
- 手工打开任务详情确认 timeline 显示。
