# 前端技术评审 v8.6.2 — 无前端变更

> 版本 v8.6.2，一旦确定尽量不要轻易改动。基线 v8.6.1。

本版本为纯后端改动（任务 9.5–9.8：LLM 出参 schema 契约校验），**前端无代码变更**。

- 默认 observe 模式下所有接口行为与 v8.6.1 完全一致（全量回归证明）。
- enforce 模式的重试发生在后端 LLM 调用层，SSE 事件序列不变（progress 文案可能多一轮）。
