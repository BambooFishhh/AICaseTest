# 后端技术评审 v3.8 — 树状用例列表

**日期**: 2026-08-11
**基线**: v3.7

---

## 无后端改动

本迭代为纯前端改动。后端 API 已返回 `module`、`preconditions`、`steps`、`expectedResults` 字段，无需修改。

- `GET /api/projects/{projectId}/testcases` — 已支持 module/type/keyword/reviewStatus 筛选
- `TestCaseDTO` — 已包含 `preconditions`/`steps`/`expectedResults` 字段（List<String>）
- 前端通过 `pageSize=9999` 加载全部用例用于树状分组

---

## 测试验证

```powershell
$env:JAVA_HOME="C:\Users\DislikeTomato\.jdks\ms-17.0.18"
$env:Path="$env:JAVA_HOME\bin;$env:Path"
cd backend; mvn compile "-Dmaven.repo.local=../.m2-repo" 2>&1 | Select-Object -Last 5
```

预期：BUILD SUCCESS（无源文件变更，仅验证回归）
