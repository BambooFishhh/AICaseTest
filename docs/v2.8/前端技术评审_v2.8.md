# 前端技术评审 v2.8 — 执行链路切换

> 版本：v2.8 | 基线：v2.7 | 一旦确定尽量不要轻易改动

## 无前端改动

v2.8 是后端执行链路切换，前端不改动。

- 前端 API 调用不变
- 截图展示不变（screenshotBefore/After 字段不变）
- 录屏回放仍用旧图片轮播（recordingFrames 字段可能为空，v2.9 升级为视频播放）

## 验证

前端无需重新构建。后端切换后，截图展示正常，录屏回放区域可能为空（v2.9 补视频播放）。
